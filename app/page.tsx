export const metadata = { title: 'ConnectNetwork — Connecting Products. Connecting People.', alternates: { canonical: 'https://connectnetwork.co.za/' } } as const

import HomeClient from '@/components/pages/HomeClient'

export default function HomePage() {
  return <HomeClient />
}
