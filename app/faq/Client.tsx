'use client'
import { useState } from 'react'
const faqs = [['What is ConnectNetwork?', 'ConnectNetwork is an online marketplace where customers can discover products from approved suppliers.'], ['Who sells the products?', 'Independent suppliers list products on ConnectNetwork. They remain responsible for accurate product information, stock, and fulfilment.'], ['How do orders work?', 'You select a product, pay through ConnectNetwork, and the supplier prepares and delivers the order.'], ['Can I become a supplier?', 'Yes. Apply through the supplier page. We review applications before approving suppliers and publishing products.'], ['What are sponsored products?', 'Suppliers can pay for extra visibility. Sponsored products and businesses are labelled clearly so shoppers know they are paid placements.']]
export default function FAQClient() { const [open, setOpen] = useState<number | null>(null); return <div className="mt-8 space-y-4">{faqs.map(([question, answer], index) => <article key={question} className="card"><button className="flex w-full items-center justify-between p-5 text-left font-semibold" onClick={() => setOpen(open === index ? null : index)} aria-expanded={open === index}>{question}<span>{open === index ? '–' : '+'}</span></button>{open === index && <p className="border-t px-5 py-5 text-slate-600">{answer}</p>}</article>)}</div> }
